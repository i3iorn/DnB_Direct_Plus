from setuptools import setup

setup(
   name='DirectPlus',
   version='0.2.13',
   description='Accesssing the API',
   author='Björn Schrammel',
   author_email='github@schrammel.dev',
   packages=['DirectPlus'],  # would be the same as name
)
